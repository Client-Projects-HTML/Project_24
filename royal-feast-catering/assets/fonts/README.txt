Fonts are loaded from Google Fonts by default (see the @import at the top of assets/css/style.css).
If you need to self-host fonts (e.g. for GDPR/offline use), place .woff2 files here and update the @font-face rules accordingly.
